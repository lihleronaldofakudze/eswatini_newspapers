name: eswatini_newspapers
description: Find All The Relavate News That Trend Around ESwatini.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  animated_splash_screen: ^1.1.0
  awesome_dialog: ^2.2.0
  carousel_slider: ^4.0.0
  cloud_firestore: ^3.1.11
  cupertino_icons: ^1.0.2
  firebase_core: ^1.3.0
  flutter:
    sdk: flutter
  google_fonts: ^2.1.0
  provider: ^6.0.2
  url_launcher: ^6.0.20
  webview_flutter: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/
    - icons/
